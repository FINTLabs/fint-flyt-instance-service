apiVersion: apps/v1
kind: Deployment
metadata:
  name: fint-flyt-instance-service
spec:
  replicas: 1
  strategy:
    type: Recreate
  template:
    spec:
      restartPolicy: Always
      volumes:
        - name: credentials
          secret:
            secretName: fint-flyt-instance-service-kafka-certificates
      containers:
        - image: ghcr.io/fintlabs/fint-flyt-instance-service:sha-0000000
          name: fint-flyt-instance-service
          volumeMounts:
            - name: credentials
              mountPath: /credentials
          resources:
            limits:
              memory: "2048Mi"
              cpu: "1"
            requests:
              memory: "512Mi"
              cpu: "100m"
          ports:
            - containerPort: 8080
          envFrom:
            - secretRef:
                name: fint-flyt-instance-service-kafka
            - secretRef:
                name: fint-flyt-instance-service
            - configMapRef:
                name: fint-flyt-instance-service